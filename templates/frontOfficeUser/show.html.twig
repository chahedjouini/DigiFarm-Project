{% extends 'base_front_etude.html.twig' %}



{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.Prenom }}</td>
            </tr>
            <tr>
                <th>AdresseMail</th>
                <td>{{ user.AdresseMail }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.Password }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.Role.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_editmine', {'id': user.id}) }}">edit</a>

    {{ include('frontOfficeUser/_delete_form.html.twig') }}
{% endblock %}

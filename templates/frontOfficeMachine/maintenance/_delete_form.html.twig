<form method="post" action="{{ path('app_maintenance_delete', {'id': maintenance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" novalidate>
    <button class="btn btn-danger" style="width: 150px;">Delete</button>
</form>

{% extends 'back/base.html.twig' %}

{% block title %}Technicien index{% endblock %}

{% block body %}
    <h1>Technicien index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Prenom</th>
                <th>Specialite</th>
                <th>Email</th>
                <th>Telephone</th>
                <th>Localisation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for technicien in techniciens %}
            <tr>
                <td>{{ technicien.id }}</td>
                <td>{{ technicien.name }}</td>
                <td>{{ technicien.prenom }}</td>
                <td>{{ technicien.specialite }}</td>
                <td>{{ technicien.email }}</td>
                <td>{{ technicien.telephone }}</td>
                <td>{{ technicien.localisation }}</td>
                <td>
                    <a href="{{ path('app_technicien_show', {'id': technicien.id}) }}">show</a>
                    <a href="{{ path('app_technicien_edit', {'id': technicien.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_technicien_new') }}">Create new</a>
{% endblock %}

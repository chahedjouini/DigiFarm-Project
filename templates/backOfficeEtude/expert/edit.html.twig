{% extends 'base_back_etude.html.twig' %}

{% block title %}Edit Expert{% endblock %}

{% block body %}
    <h1>Edit Expert</h1>

    <!-- Formulaire d'édition de l'expert -->
    {{ include('backOfficeEtude/expert/_form.html.twig', {'button_label': 'Update'}) }}

  <div style="text-align: center; margin-top: 20px;">
            <a href="{{ path('app_expert_index',{'context': app.request.get('context')}) }}" class="button">Back to List</a>
        </div>


    <!-- Formulaire de suppression -->
    <div style="text-align: center; margin-top: 20px;">
        {{ include('backOfficeEtude/expert/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% extends 'base_back_etude.html.twig' %}

{% block title %}Edit Culture{% endblock %}

{% block body %}
    <h1>Edit Culture</h1>

   
    {{ include('backOfficeEtude/culture/_form.html.twig', {'button_label': 'Update'}) }}

<div style="text-align: center; margin-top: 20px;">
    <a href="{{ path('app_culture_index', {'context': context}) }}" class="button">Back to list</a>
</div>


    
    <div style="text-align: center; margin-top: 20px;">
        {{ include('backOfficeEtude/culture/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Etude{% endblock %}

{% block body %}
    <h1>Etude</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etude.id }}</td>
            </tr>
            <tr>
                <th>Id_user</th>
                <td>{{ etude.idUser }}</td>
            </tr>
            <tr>
                <th>Type_etude</th>
                <td>{{ etude.typeEtude }}</td>
            </tr>
            <tr>
                <th>Date_r</th>
                <td>{{ etude.dateR ? etude.dateR|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Cout</th>
                <td>{{ etude.cout }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etude_index') }}">back to list</a>

    <a href="{{ path('app_etude_edit', {'id': etude.id}) }}">edit</a>

    {{ include('etude/_delete_form.html.twig') }}
{% endblock %}

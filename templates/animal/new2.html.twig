{% extends 'base_back_etude.html.twig' %}

<title>New Animal</title>

{% block body %}
    <h1>Create new Animal</h1>

    {{ include('animal/_form.html.twig') }}

    <a href="{{ path('app_animal_index') }}">back to list</a>
{% endblock %}

<form method="post" action="{{ path('app_veterinaire2_delete', {'id': veterinaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veterinaire.id) }}">
    <button class="btn btn-danger" style="margin-top: 10px;">Delete</button>
</form>

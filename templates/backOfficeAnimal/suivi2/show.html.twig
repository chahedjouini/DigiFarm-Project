{% extends 'base_back_etude.html.twig' %}

{% block body %}
    <div class="container mt-4 pt-5">
        <h1 class="text-center text-primary mb-4">Suivi</h1>

        <div class="card shadow-lg p-4">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th class="bg-light">Id</th>
                        <td>{{ suivi.id }}</td>
                    </tr>
                    <tr>
                        <th class="bg-light">Temperature</th>
                        <td>{{ suivi.temperature }}</td>
                    </tr>
                    <tr>
                        <th class="bg-light">Rythme cardiaque</th>
                        <td>{{ suivi.rythmeCardiaque }}</td>
                    </tr>
                    <tr>
                        <th class="bg-light">Etat</th>
                        <td>{{ suivi.etat }}</td>
                    </tr>
                    <tr>
                        <th class="bg-light">Veterinaire</th>
                        <td>{{ suivi.veterinaire.id }}</td>
                    </tr>
                    <tr>
                        <th class="bg-light">Id client</th>
                        <td>{{ suivi.idClient }}</td>
                    </tr>
                </tbody>
            </table>

            <div class="d-flex justify-content-center gap-3 mt-4">
                <a href="{{ path('app_suivi2_index') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour à la liste
                </a>
                <a href="{{ path('app_suivi2_edit', {'id': suivi.id}) }}" class="btn btn-warning">
                    <i class="fas fa-edit"></i> Modifier
                </a>
            </div>

            <div class="d-flex justify-content-center mt-3">
                {{ include('backOfficeAnimal/suivi2/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

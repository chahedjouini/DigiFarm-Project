<form method="post" action="{{ path('app_maintenance2_delete', {'id': maintenance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" novalidate>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance.id) }}">
    <button class="btn">Delete</button>
</form>
